# function loop_function
# this function shall be executed in a repeating command block

# Bridge Snowball implementation
execute at @e[type=snowball] run setblock ~ ~-2 ~ minecraft:white_wool keep

# kill all entities under y=160
kill @e[y=0,dy=160]

# forbid empty buckets
clear @a minecraft:bucket
